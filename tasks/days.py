import datetime

class Days():
    now = datetime.datetime.now()
    two = datetime.datetime.now() + datetime.timedelta(days=1)
    three = datetime.datetime.now() + datetime.timedelta(days=2)
    four = datetime.datetime.now() + datetime.timedelta(days=3)
    five = datetime.datetime.now() + datetime.timedelta(days=4)
    six = datetime.datetime.now() + datetime.timedelta(days=5)
    seven = datetime.datetime.now() + datetime.timedelta(days=6)
